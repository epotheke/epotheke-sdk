compile:
    stage: compile
    image: public.docker.ecsec.de/ecsec/tools/cimg-android-jdk25:2025.10
    tags:
        - "podman"
    cache:
    - key: "$CACHE_KEY"
      paths: !reference [ .cache-gradle-base, paths ]
      policy: pull-push
    - key: "$CACHE_TMP_KEY"
      paths: [ .cache-gradle-builds, paths ]
      policy: pull-push
    script:
        - !reference [.versioning, script]
        - ./gradlew assemble
